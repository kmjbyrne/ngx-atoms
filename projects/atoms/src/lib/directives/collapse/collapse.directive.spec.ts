import { CollapseDirective } from './collapse.directive';

describe('CollapseDirective', () => {
    it('should create an instance', () => {
        const directive = new CollapseDirective(null);
        expect(directive).toBeTruthy();
    });
});
